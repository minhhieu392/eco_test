// const express = require('express');
// const productSchema = require('../validations/productSchema');
// const productControler = require('../controllers/productControler')
// const validate = require('../middleware/validation');
// const router = express.Router();

// router.get('/new',validate.body(productSchema.createShop), productControler.handle_get_top);

// router.get('/search', validate.query(productSchema.search), productControler.handle_search);

// export default router;